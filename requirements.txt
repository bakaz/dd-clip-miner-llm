# 兼容传统 pip 流程；权威依赖列表见 pyproject.toml [project.dependencies]
# 推荐：pip install -e .

PyYAML>=6.0.2
tqdm>=4.66
imageio-ffmpeg>=0.5.1
faster-whisper>=1.2.1
numpy>=1.26
soundfile>=0.12
openai>=1.0.0
httpx>=0.23.0
ddgs>=9.0.0

# 测试：pip install -e ".[test]"
# FunASR：pip install -e ".[funasr]"
# GPU（CUDA 12）：pip install -r requirements-cu12.txt