name: tests

# Offline unit tests only — no config.yaml, API keys, or local media required.
on:
  push:
    branches: [master, main]
  pull_request:
    branches: [master, main]
  workflow_dispatch:

concurrency:
  group: tests-${{ github.ref }}
  cancel-in-progress: true

jobs:
  pytest:
    name: pytest (py${{ matrix.python-version }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.11", "3.12"]

    env:
      DD_CLIP_MINER_LLM_CI: "1"
      LLM_API_KEY: ""
      MIMO_API_KEY: ""
      DEEPSEEK_API_KEY: ""
      PYTEST_ADDOPTS: -p no:cacheprovider

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y ffmpeg mkvtoolnix libsndfile1

      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install --no-cache-dir -e ".[test]"

      - name: Verify editable install
        run: |
          python -c "from dd_clip_miner_llm import ffmpeg; from dd_clip_miner_llm.concat.models import ProblemProfile; p = ffmpeg.classify_ffmpeg_output('segment(s) 2'); assert p.bitstream_corrupt_indexes == [1], p"
          python -m pytest tests --collect-only -q

      - name: Compile package
        run: python -m compileall -q dd_clip_miner_llm tests

      - name: Run unit tests
        run: |
          mkdir -p .tmp/pytest
          python -m pytest tests -q --basetemp=.tmp/pytest --tb=short