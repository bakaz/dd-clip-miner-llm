"""Setuptools entry point — package metadata lives in pyproject.toml.

For interactive environment setup (venv, ffmpeg, FunASR, etc.) use:
    python install.py
    python setup_env.py
"""
from setuptools import setup

setup()